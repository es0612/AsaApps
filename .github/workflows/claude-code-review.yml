name: AsaApps SwiftUI Code Review

on:
  pull_request:
    types: [opened, synchronize, reopened]
    # SwiftUI/iOS開発関連ファイルのみを対象
    paths:
      - "Apps/**/*.swift"
      - "Shared/**/*.swift"
      - "**/*.xcodeproj/**"
      - "**/*.xcworkspace/**"
      - "**/*.plist"
      - "**/*.xcassets/**"

jobs:
  claude-review:
    # Optional: Filter by PR author
    # if: |
    #   github.event.pull_request.user.login == 'external-contributor' ||
    #   github.event.pull_request.user.login == 'new-developer' ||
    #   github.event.pull_request.author_association == 'FIRST_TIME_CONTRIBUTOR'
    
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
      issues: read
      id-token: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Run Claude Code Review
        id: claude-review
        uses: anthropics/claude-code-action@beta
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}

          # Optional: Specify model (defaults to Claude Sonnet 4, uncomment for Claude Opus 4.1)
          # model: "claude-opus-4-1-20250805"

          # AsaApps SwiftUI専用レビュープロンプト
          direct_prompt: |
            AsaAppsプロジェクト（朝活パパエンジニアの100個SwiftUIアプリプロジェクト）のコードレビューを実施してください。
            日本語でレビューを提供し、以下の観点で確認してください：

            **SwiftUI/iOS開発のベストプラクティス:**
            - SwiftUIライフサイクルの適切な使用（@State、@StateObject、@Observable）
            - パフォーマンス最適化（不要な再描画の回避、@Binding使用）
            - iOS設計パターンの遵守（MVVM、Clean Architecture）
            - アクセシビリティの考慮（VoiceOver対応）

            **AsaAppsプロジェクト固有の要件:**
            - ブランドガイドライン準拠（AsaCoffeeBrown、AsaMocha等のカラー使用）
            - 共有コンポーネントの適切な利用（AsaButton、AsaCard、AsaLaunchScreen）
            - コメントとドキュメントの日本語化
            - 家族向け・生産性重視のテーマ一貫性

            **コード品質と安全性:**
            - Swiftのベストプラクティス（Optional処理、エラーハンドリング）
            - セキュリティ考慮（個人情報処理、HealthKitアクセス）
            - メモリ管理とパフォーマンス

            建設的で学習に役立つフィードバックを心がけてください。

          # Optional: Use sticky comments to make Claude reuse the same comment on subsequent pushes to the same PR
          # use_sticky_comment: true
          
          # Optional: Customize review based on file types
          # direct_prompt: |
          #   Review this PR focusing on:
          #   - For TypeScript files: Type safety and proper interface usage
          #   - For API endpoints: Security, input validation, and error handling
          #   - For React components: Performance, accessibility, and best practices
          #   - For tests: Coverage, edge cases, and test quality
          
          # Optional: Different prompts for different authors
          # direct_prompt: |
          #   ${{ github.event.pull_request.author_association == 'FIRST_TIME_CONTRIBUTOR' && 
          #   'Welcome! Please review this PR from a first-time contributor. Be encouraging and provide detailed explanations for any suggestions.' ||
          #   'Please provide a thorough code review focusing on our coding standards and best practices.' }}
          
          # SwiftUI/iOS開発で使用するツールを許可
          allowed_tools: "Bash(xcodebuild -project * -scheme * clean build),Bash(swift --version),Bash(swiftlint),Bash(xcrun simctl list),Bash(plutil -lint *)"
          
          # Optional: Skip review for certain conditions
          # if: |
          #   !contains(github.event.pull_request.title, '[skip-review]') &&
          #   !contains(github.event.pull_request.title, '[WIP]')

